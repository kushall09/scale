[package]
name = "scale"
version = "0.1.0"
edition = "2026"
author = "kushal"
description = "this converts measurements into scale"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
